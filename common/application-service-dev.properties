# 모든 서비스 dev 환경 공통 설정
logging.level.org.springframework=INFO
logging.level.org.mybatis=INFO

# Eureka Client 공통 설정 (cloud 프로필에서만 활성화)
eureka.client.service-url.defaultZone=${EUREKA_SERVER:http://localhost:8761}/eureka/
eureka.instance.prefer-ip-address=true
eureka.instance.instance-id=${spring.application.name}:${server.port}

spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Actuator 설정 (Dev 환경: 전체 노출)
management.endpoints.web.exposure.include=health,info,metrics,prometheus,refresh,bus-refresh,env
management.endpoint.health.show-details=when_authorized
management.prometheus.metrics.export.enabled=true

# JWT Secret (Dev: 개발 서버 전용 키)
jwt.secret=${JWT_SECRET:NVp7zZgjqxoVUd1kVDNDDZKtP5jVb3xSr4jlGzpSTkD91RzlrR0VFbnsBlbhfxzvRjlUVXqgJXj3Qv412jGuzY}

# JPA
spring.jpa.hibernate.ddl-auto=false
spring.jpa.show-sql=true
# Dialect는 각 db-*.properties 파일에서 정의함
spring.jpa.properties.hibernate.format_sql=true
